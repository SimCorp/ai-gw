"use client";
// TeamSelector is no longer used in the sidebar (team selection moved to PortalShell).
// This file is kept as a no-op export so any stray imports don't break the build.
export default function TeamSelector() { return null; }
