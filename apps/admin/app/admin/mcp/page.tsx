import { ComingSoon } from '../_components/PageStates';

export default function McpPage() {
  return (
    <ComingSoon
      title="MCP servers"
      description="Manage and monitor Model Context Protocol servers registered in the gateway. View tool counts, call volumes, latency, and approval status per server."
    />
  );
}
