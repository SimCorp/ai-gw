import { ComingSoon } from '../_components/PageStates';

export default function ApprovalsPage() {
  return (
    <ComingSoon
      title="Approvals"
      description="Review and approve requests for expanded tool scopes, skill publishes, and policy exceptions. Approval workflows will route here based on team-level risk thresholds."
    />
  );
}
