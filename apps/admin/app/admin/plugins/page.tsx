import { ComingSoon } from '../_components/PageStates';

export default function PluginsPage() {
  return (
    <ComingSoon
      title="Plugins"
      description="Control which first-party and community plugins are available to developers. Set org-wide policy gates, opt-in scopes, and per-team overrides."
    />
  );
}
