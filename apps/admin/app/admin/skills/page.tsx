import { ComingSoon } from '../_components/PageStates';

export default function SkillsPage() {
  return (
    <ComingSoon
      title="Skills"
      description="Browse and govern published agent skills across the organisation. Review versions, model usage, and promotion status from draft through to org-wide availability."
    />
  );
}
