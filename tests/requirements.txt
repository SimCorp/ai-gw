# Integration test dependencies for the AI Gateway test suite.
# Pin patch versions loosely so minor upstream fixes are picked up automatically.

pytest>=8.3,<9
pytest-asyncio>=0.24,<1
pytest-playwright>=0.5,<1
httpx>=0.27,<1
anyio[trio]>=4.6,<5
playwright>=1.44,<2
