{
  "name": "aigw-e2e",
  "version": "0.0.1",
  "private": true,
  "description": "On-demand browser E2E quality walkthrough of the AI Gateway portals. Standalone — NOT part of the pnpm workspace, so it never enters the gating `turbo test`.",
  "scripts": {
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:install": "playwright install chromium",
    "report": "playwright show-report",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.0",
    "typescript": "^5.8.0"
  }
}
